This folder contains files in C that handles files, external files to be specific

Open -> being a C library function which we use to open a file with either existing or non existing
if not exisiting open function creates one

CLOSE -> we use it to close a file that we opened using the open function to signal that we are done with the file

3 STANDARD POSIX of C

1)stdin
2) stderror
3) stdout

